def calculation(length,breadth)
    if str.isdigit(length)andstr.isdigit(breadth)
        return length x breadth.
    else
        print("please enter a value")
        return false
    calculateArea(5,7)
